apiVersion: v1
kind: Service
metadata:
  name: hermes
  namespace: apps
spec:
  selector:
    app: hermes
  ports:
    - name: api
      port: 8642
      targetPort: api
      protocol: TCP
    - name: dashboard
      port: 9119
      targetPort: dashboard
      protocol: TCP
